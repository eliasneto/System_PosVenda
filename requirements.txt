Django==5.2.16
python-decouple==3.8
python-dotenv==1.2.2
gunicorn==23.0.0
mysqlclient==2.2.8
openpyxl==3.1.5
reportlab==4.4.4
requests==2.32.3
# FEAT-027 (RN-043/RN-044): autenticacao via Active Directory (ADR-002) -
# mesmas versoes ja usadas no modulo-posVenda; exige libldap2-dev/
# libsasl2-dev no Dockerfile pra compilar o python-ldap.
python-ldap==3.4.3
django-auth-ldap==4.8.0

# FEAT-033 (Fase 1, ADR-004/RN-056): RPA de anexo de Nota Fiscal no portal
# EACE (nucleo em apps/integracoes/eace/, reaproveitado do prototipo
# doc/auto_eace_nf_servidor). Depois de instalar, rodar tambem
# 'python -m playwright install chromium' (baixa o navegador - nao vem
# pelo pip); no Dockerfile de producao isso ainda precisa ser feito pelo
# DevOps (ADR-004, Pendencias).
playwright==1.60.0
# FEAT-033/RN-057: extrai INEP/Produto/Valor de dentro do PDF da Nota
# Fiscal, para conferir contra o valor exibido no portal antes do upload.
pdfplumber==0.11.6
